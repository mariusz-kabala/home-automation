export enum alertLevels {
    low = 'low',
    medium = 'medium',
    high = 'high'
}

export enum alertTypes {
    deviceAvailability = 'deviceAvailability',
    forecast = 'forecast',
    sensors = 'sensors'
}
